{{ 'section-nutrition-foundation.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .nutrition-foundation {
    background-color: {{ section.settings.background_color }};
  }

  .nutrition-foundation__heading {
    color: {{ section.settings.heading_color }};
    font-size: {{ section.settings.heading_size }}px;
  }

  .nutrition-foundation__description {
    color: {{ section.settings.text_color }};
  }

  .nutrition-foundation__key-points-heading {
    color: {{ section.settings.heading_color }};
  }

  .nutrition-foundation__percentage {
    color: {{ section.settings.accent_color }};
    font-size: {{ section.settings.percentage_size }}px;
  }

  .nutrition-foundation__point-text {
    color: {{ section.settings.text_color }};
  }

  .nutrition-foundation__button {
    background-color: {{ section.settings.button_color }};
    color: {{ section.settings.button_text_color }};
  }

  .nutrition-foundation__button:hover {
    background-color: {{ section.settings.button_hover_color }};
  }

  .nutrition-foundation__divider {
    border-color: {{ section.settings.divider_color }};
  }
{%- endstyle -%}

<div class="nutrition-foundation section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="nutrition-foundation__grid">
      <!-- Content Container -->
      <div class="nutrition-foundation__content">
        {%- if section.settings.heading != blank -%}
          <h2 class="nutrition-foundation__heading">
            {{ section.settings.heading }}
          </h2>
        {%- endif -%}

        {%- if section.settings.description != blank -%}
          <p class="nutrition-foundation__description">
            {{ section.settings.description }}
          </p>
        {%- endif -%}

        {%- if section.settings.key_points_heading != blank -%}
          <h3 class="nutrition-foundation__key-points-heading">
            {{ section.settings.key_points_heading }}
          </h3>
        {%- endif -%}

        <!-- Key Points -->
        <div class="nutrition-foundation__points">
          {%- for block in section.blocks -%}
            {%- if block.type == 'key_point' -%}
              <div class="nutrition-foundation__point" {{ block.shopify_attributes }}>
                <span class="nutrition-foundation__percentage">{{ block.settings.percentage }}%</span>
                <p class="nutrition-foundation__point-text">{{ block.settings.text }}</p>
              </div>
              {%- unless forloop.last -%}
                <div class="nutrition-foundation__divider"></div>
              {%- endunless -%}
            {%- endif -%}
          {%- endfor -%}
        </div>

        {%- if section.settings.button_text != blank -%}
          <a href="{{ section.settings.button_link }}" class="nutrition-foundation__button button">
            {{ section.settings.button_text }}
          </a>
        {%- endif -%}
      </div>

      <!-- Image Container -->
      <div class="nutrition-foundation__image-container">
        {%- if section.settings.image != blank -%}
          <img src="{{ section.settings.image | image_url: width: 570 }}" 
               alt="{{ section.settings.image.alt | escape }}"
               width="570" 
               height="570"
               class="nutrition-foundation__image">
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Nutrition Foundation",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "Nutrition is the foundation for longer, healthier lives in dogs.",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "description",
      "default": "Invest in your dog's future with our scientifically formulated superfood-powered supplements. Give them the nutrition they deserve and watch them thrive with vitality, energy, and the joy of a longer, healthier life.",
      "label": "Description"
    },
    {
      "type": "text",
      "id": "key_points_heading",
      "default": "Key Points:",
      "label": "Key points heading"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Product image"
    },
    {
      "type": "text",
      "id": "button_text",
      "default": "Give your furry friend the gift of wholesome nutrition",
      "label": "Button text"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#f9fafb"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#161723"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#424153"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Percentage color",
      "default": "#f97316"
    },
    {
      "type": "color",
      "id": "divider_color",
      "label": "Divider color",
      "default": "#e3e3e8"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button color",
      "default": "#f97316"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_hover_color",
      "label": "Button hover color",
      "default": "#ea580c"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Heading size",
      "default": 40
    },
    {
      "type": "range",
      "id": "percentage_size",
      "min": 20,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Percentage size",
      "default": 32
    },
    {
      "type": "header",
      "content": "Section padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "key_point",
      "name": "Key Point",
      "settings": [
        {
          "type": "number",
          "id": "percentage",
          "default": 97,
          "label": "Percentage"
        },
        {
          "type": "textarea",
          "id": "text",
          "default": "Dogs choose our dog food over leading brands because of its real functional ingredients and delicious flavor.",
          "label": "Point description"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Nutrition Foundation",
      "blocks": [
        {
          "type": "key_point",
          "settings": {
            "percentage": 97,
            "text": "Dogs choose our dog food over leading brands because of its real functional ingredients and delicious flavor."
          }
        },
        {
          "type": "key_point",
          "settings": {
            "percentage": 84,
            "text": "Our dog food provides superior nutrition and a patented probiotic for optimal nutrient absorption."
          }
        },
        {
          "type": "key_point",
          "settings": {
            "percentage": 92,
            "text": "Our dog food's high protein and fat digestibility contribute to ideal stool quality."
          }
        }
      ]
    }
  ]
}
{% endschema %}
